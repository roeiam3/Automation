Automation Ideas - 

1 - Use LLDP to configure on every UP/UP interface the description of where it goes via LLDP.

Be ideompotent

Do it yourself!


Process - 

1 - cry 
2 - Enable (If not enabled) LLDP on all interfaces Globally
3 - Wait X (Default is 30) Amount of time for LLDP to work
4 - use "show lldp neighbors" to registar (And debug for me) to identify the interfaces that need to be configured
5 - somehow pasrse the YML output, and identify interfaces
6 - run a loop on the interfaces and configure for each interface a description to the neighbor 

Use Device ID (For example R6) to Local Intf (E0/0) 




Device ID           Local Intf     Hold-time  Capability      Port ID
R6                  Et0/0          120        R               Et0/1

Example of  how it looks.
pip install ansible-pylibssh (useful for better ssh + no errors?)

=====================================================

2 - use LLDP to create a map of devices? (Drawing a topology via Draw.io maybe?)
(Skip for now, need real hardware)

Process - 

1 - Run LLDP Globally (Set timers for 5 seconds, for faster speeds)
2 - wait 5 seconds
3 - Fetch " Show ip lldp neighbors " that information is sufficient


=====================================================

3 - Automatic saving of running-config, uploading to FTP server, comparing old Startup to new startup?

(Can't do on IOL, need hardware)

1 - run a saving script (copy running-config startup-config) 
1.5 - (Probably) Identify the most recent save (For later comparison)
2 - copy from X in inventory to Location on server (a Copy x: to X: command), Crucially, in the saving, Append a date
3 - After saving with a date (Include seconds), Compare Newest save to the save before it (Meaning not the earliest, but the one before it)
4 - if they're the same, Ignore and continue, If they're different, Generate a report in a specific folder, using the name of the device and the date, that report card will paste code that is different between the two ( For example, Descrptions, Neighbor command etc..) anything that is different will be there
5 - in the end Playbook reports Which devices in inventroy were successful, which failed and which had changes and a report was made for.

=====================================================

4 - 
